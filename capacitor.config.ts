import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'me.paullacaille.photoapp',
  appName: 'photo-gallery',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
